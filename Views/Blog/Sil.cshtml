@model KurumsalWeb.Models.Model.Blog
@{
    ViewBag.Title = "Sil";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2><i class="fas fa-pencil-alt"></i> Yazı Sil</h2>
<hr />

<section class="panel">
    <header class="panel-heading">
        Yazı Sil
    </header>
    <div class="panel-body">
        <div class="alert alert-danger fade in">
            <h2><strong>@Model.Baslik</strong> İsimli Yazıyı Silmek İstediğinize Eminmisiniz?</h2>
        </div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.Id)
            <input type="submit" value="Sil" class="btn btn-danger" />
            <a href="/Blog" class="btn btn-primary"><i class="fas fa-backward"></i> Listeye Dön</a>
        }
    </div>
</section>


